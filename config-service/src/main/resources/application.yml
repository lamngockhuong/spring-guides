spring:
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config
  profiles:
    active: native
  security:
    user:
      name: ngockhuong
      password: 123456

server:
  port: 8888
